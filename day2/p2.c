#include <stdio.h>

int main(){
	printf("%d", printf("Ramya\n"));
	return 0;
}
Input function
	scanf()
	
	int scanf(const char *format, address1, address2,...);
	
	it returns number of input getting from scanf()
	
	syntax of format
	
		%[*][width][h|l|L] <type-character>
